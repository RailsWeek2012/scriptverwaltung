
<%= link_to('Edit', edit_script_path(script), class: "btn btn-mini") if isAdmin? or !isActiv?(script) %>
<%= link_to('Destroy', script, method: :delete, confirm: 'Are you sure?', class: "btn btn-mini btn-danger") if isAdmin? or !isActiv?(script) %>
<%= link_to('Freigeben', activate_script_path(script), confirm: 'Are you sure?', class: "btn btn-mini btn-success") if isAdmin? and !isActiv?(script) %>

